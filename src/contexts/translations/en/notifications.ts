const notifications = {
  kmOverdue: 'Overdue — service now!',
  kmRemaining: '~%{km} km remaining',
  dateDueToday: 'Due today!',
  dateDueIn: 'Due in %{days} day(s)',
} as const;

export default notifications;
